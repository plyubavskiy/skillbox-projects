import math

print('Введите координаты объекта:')
x = float(input('X: '))
y = float(input('Y: '))
r = float(input('Введите радиус поиска: '))
z = math.sqrt(x**2 + y**2)

if z <= r:
    print('Объект где-то рядом')
else:
    print('Объекта в области нет')
